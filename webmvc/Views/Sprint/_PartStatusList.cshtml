@model Epiworx.WebMvc.Models.SprintFormModel
<div class="part">
    <div class="wrapper">
        <h3>
            Statuses</h3>
        <ul>
            @foreach (var status in Model.Statuses)
            {
                <li>@status.Name</li>
            }
        </ul>
    </div>
</div>
