@model Epiworx.WebMvc.Models.SprintFormModel
@using Epiworx.WebMvc.Helpers
<ul class="commands">
    <li>
        <button>
            Save Changes</button></li>
    <li><span>or</span></li>
    @if (!Model.Sprint.IsNew)
    { 
        <li>@Html.ActionLink("Delete", "Delete", new { id = Model.Sprint.SprintId })</li>
    }
    <li><a href="@if (Model.Sprint.IsNew) {<text>@Url.Action("Index", "Home")</text> } else {<text>@Url.Action("Details", new { id = Model.Sprint.SprintId})</text>}">Back</a></li>
</ul>
