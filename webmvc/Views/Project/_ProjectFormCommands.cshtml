@model Epiworx.WebMvc.Models.ProjectFormModel
@using Epiworx.WebMvc.Helpers
<ul class="commands">
    <li>
        <button>
            Save Changes</button></li>
    <li><span>or</span></li>
    @if (!Model.Project.IsNew)
    { 
        <li>@Html.ActionLink("Delete", "Delete", new { id = Model.Project.ProjectId })</li>
    }
    <li><a href="@if (Model.Project.IsNew) {<text>@Url.Action("Index", "Home")</text> } else {<text>@Url.Action("Details", new { id = Model.Project.ProjectId})</text>}">Back</a></li>
</ul>
